To Run this follow the below steps :
Connect to HPSA
Select set of servers (on MRS only)
Right click and to go "run script"
Select 2nd check 
Run the custom script as powershell
Copy paste the script given here
run the script
Export the output in raw txt format and keep name of file as mentioned at top of script
